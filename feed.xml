<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://portfolio.rothellc.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://portfolio.rothellc.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-01-23T03:40:17+00:00</updated><id>https://portfolio.rothellc.com/feed.xml</id><title type="html">blank</title><subtitle>Senior engineer at Lockheed Martin with M.S. in Electrical Engineering from Johns Hopkins University. Specializes in FPGA firmware design with experience in electrical and mechanical hardware as well as software development. </subtitle><entry><title type="html">Xilinx/AMD Vivado SoC FPGA Development and Debug Workflow</title><link href="https://portfolio.rothellc.com/blog/2025/xilinx-soc-fpga-development-and-debug-workflow-guide/" rel="alternate" type="text/html" title="Xilinx/AMD Vivado SoC FPGA Development and Debug Workflow"/><published>2025-10-18T00:00:00+00:00</published><updated>2025-10-18T00:00:00+00:00</updated><id>https://portfolio.rothellc.com/blog/2025/xilinx-soc-fpga-development-and-debug-workflow-guide</id><content type="html" xml:base="https://portfolio.rothellc.com/blog/2025/xilinx-soc-fpga-development-and-debug-workflow-guide/"><![CDATA[<p><img src="../../../assets/img/diagrams/2025-10-17-xilinx-soc-fpga-development-and-debug-workflow-guide/zynq-7000-soc.png" alt="Zynq 7000 SoC Block Diagram" class="img-fluid"/> <em>Source: AMD/Xilinx Zynq-7000 SoC Data Sheet: Overview DS190 (v1.11.1) July 2, 2018, Figure 1</em></p> <hr/> <p>Verification of an FPGA design post-synthesis involves several steps, which can be incrementally worked through as the design matures. The following guide provides a decent outline to carry a design from post-simulation all the way to implementation alongside a SoC processor that can control and read the FPGA from software. I originally wrote this guide while working on my Master’s degree, and revised it into a checklist as I found myself doing the same workflows. In situations where one missed step could require you to endlessly debug a phantom issue, it is helpful to have a repeatable process. While this guide is not all-encompassing, it functions as an excellent base framework for a junior FPGA designer to work from.</p> <hr/> <h1 id="fpga-overview-written-for-non-fpga-developers">FPGA Overview (Written for Non-FPGA Developers)</h1> <p>FPGAs are programmable hardware devices that are made up of a grid of repeating programmable logic blocks called “slices” and various other interlinked components, which have rapidly increased in complexity and performance in recent years. A modern Zynq Ultrascale+ MPSoC has two processors (APU and RPU), a GPU, various on-chip memory types (RAM, block RAM, distributed RAM), DMA controllers, serial transceivers, and dedicated I/O interfaces. On the boards they are mounted on, you will also often see additional memory types (DDR3/4), ADC and DACs, I/O peripherals, and more.</p> <p>Slices vary based on hardware but usually have the same general types of components. Referred to as Configurable Logic Blocks (CLBs) on the Ultrascale MPSoC, they are made up of Look-Up Tables (LUTs), flip-flops (FFs), and multiplexers. Some have additional components like adders or other logic gates thrown in.</p> <p>Newer FPGAs also have DSP slices, which are dedicated logic blocks for adders, multipliers, accumulators etc. and are instantiated differently to free up the CLBs (or, to reverse that, to free up the valuable multipliers and adders).</p> <p>When an FPGA is programmed, code such as SystemVerilog (syntactically similar to C) directs the synthesizer to connect or disconnect these slices, filling the lookup tables appropriately to achieve the coded performance on hardware. Understanding how the code synthesizes onto the FPGA is very important for the designer to know for several reasons.</p> <ol> <li>Since you are programming hardware, some code is not synthesizable as it makes no sense to the compiler to put it on hardware. Examples include “wait” statements, or the “real” datatype. It is also important to be mindful that everything happens in parallel on hardware unless otherwise specified (e.g. a clock is added), so race conditions must be avoided and typical hardware design concerns such as set up time and hold time must be considered (this is referred to as timing closure in FPGA design, and is beyond the scope of this guide).</li> <li>You will want your code to utilize the resources on the FPGA as well as hardware resources on the board, and will need to design appropriately for your device. Instead of creating a large array of registers (which will not always properly synthesize into BRAM), you generally will want to instantiate a Xilinx IP for the BRAM component on the board in order to utilize it reliably. Clock signals, rather than being generated with regular FPGA fabric logic (e.g. counters or clock dividers), should be generated from dedicated clocking primitives (PLLs, MMCMs) to avoid poor timing and other issues.</li> <li>The processor(s), if used, need to be instantiated and connected to the design. How the design will be controlled from software must be considered, and those components must also be linked and created.</li> <li>How hardware components such as DDR3/4, DACs/ADCs, and I/O peripherals need to be controlled properly, which may include both firmware instantiation of controllers as well as proper register manipulation from software.</li> <li>Building off of the previous two items, the <code class="language-plaintext highlighter-rouge">component.xml</code> file is hardware specific and must define the coded signals to the correct chip pins. Some proprietary FPGA synthesizers have a pre-configured setup, which also needs to be understood by the designer since that generally cannot be changed.</li> </ol> <p>For Xilinx FPGA design, the options are High Level Synthesis (HLS) coding in Vitis or Register Transfer Level (RTL) code in Vivado. RTL is typically preferred by electrical and FPGA engineers who need more control over hardware resources and timing, whereas HLS appeals more to software and embedded developers who can leverage C/C++ familiarity for rapid prototyping. This guide focuses on Vivado and RTL specifically, though Vitis is used in the debugging process as well.</p> <p>The following sections provide a step-by-step workflow for project creation through hardware debugging, assuming the reader has RTL source files ready for implementation.</p> <hr/> <h1 id="building-a-project">Building a Project</h1> <p>Manually create a project in Xilinx Vivado with no sources, selecting only the applicable part type. Source files are then added to the project, and sim files are added separately (as they will be considered sim-only and not for synthesis). This project build can later be automated with <code class="language-plaintext highlighter-rouge">.tcl</code> scripts, including the creation of a block diagram, once these source files are added and (if applicable) a block diagram is created. The actual construction of these source files is beyond the scope of this guide, but it assumes <code class="language-plaintext highlighter-rouge">.sv</code>, <code class="language-plaintext highlighter-rouge">.v</code>, or <code class="language-plaintext highlighter-rouge">.vhd</code> files.</p> <p>It is recommended to keep the source files linked, so that as they are modified the source files outside of the project will continuously update and can be re-added when a project is rebuilt. This may not always be desired behavior, however; especially if you have an automated <code class="language-plaintext highlighter-rouge">.tcl</code> build workflow that manages source file versions independently.</p> <p>Typically, Vivado projects themselves are not saved, as rebuilding Vivado projects is a common way to fix many errors. The project files should be added to <code class="language-plaintext highlighter-rouge">.gitignore</code> in most cases.</p> <hr/> <h1 id="timing-constraints-and-synthesisimplementation-analysis">Timing Constraints and Synthesis/Implementation Analysis</h1> <p>Before proceeding to hardware debugging, proper timing constraints need to be defined in the constraints <code class="language-plaintext highlighter-rouge">.xdc</code> file. All clocks should be defined by <code class="language-plaintext highlighter-rouge">create_clock</code> entries, and <code class="language-plaintext highlighter-rouge">set_clock_groups</code> should define all clock domain crossings. Input or output delays relative to external devices should also be defined using <code class="language-plaintext highlighter-rouge">set_input_delay</code> and <code class="language-plaintext highlighter-rouge">set_output_delay</code>, respectively. An example is shown below, which can be added to the top-level project’s constraints file.</p> <div class="language-tcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Primary clock constraints.
</span>create_clock -period 10.000 -name sys_clk_100mhz <span class="p">[</span>get_ports sys_clk<span class="p">]</span>
create_clock -period 20.000 -name ext_clk_50mhz <span class="p">[</span>get_ports ext_clk<span class="p">]</span>

<span class="c"># Zynq processor clocks are auto-constrained when using Zynq IP, but any clocks generated from custom PLLs need manual definition.
</span>create_generated_clock -name custom_clk_200mhz -source <span class="p">[</span>get_ports sys_clk<span class="p">]</span> -multiply_by 2 <span class="p">[</span>get_pins custom_pll_inst/clk_out<span class="p">]</span>

<span class="c"># Clock domain crossings - mark asynchronous clock groups which do not have a phase relationship.
</span>set_clock_groups -asynchronous -group <span class="p">[</span>get_clocks sys_clk_100mhz<span class="p">]</span> -group <span class="p">[</span>get_clocks ext_clk_50mhz<span class="p">]</span>
set_clock_groups -asynchronous -group <span class="p">[</span>get_clocks sys_clk_100mhz<span class="p">]</span> -group <span class="p">[</span>get_clocks custom_clk_200mhz<span class="p">]</span>

<span class="c"># Input delays for external devices (ADC example with 2ns setup, 1ns hold).
</span>set_input_delay -clock <span class="p">[</span>get_clocks sys_clk_100mhz<span class="p">]</span> -max 2.0 <span class="p">[</span>get_ports <span class="p">{</span>adc_data<span class="p">[</span>*<span class="p">]}]</span>
set_input_delay -clock <span class="p">[</span>get_clocks sys_clk_100mhz<span class="p">]</span> -min 1.0 <span class="p">[</span>get_ports <span class="p">{</span>adc_data<span class="p">[</span>*<span class="p">]}]</span>

<span class="c"># Output delays for external devices (DAC example with 3ns setup, 1ns hold).
</span>set_output_delay -clock <span class="p">[</span>get_clocks sys_clk_100mhz<span class="p">]</span> -max 3.0 <span class="p">[</span>get_ports <span class="p">{</span>dac_data<span class="p">[</span>*<span class="p">]}]</span>
set_output_delay -clock <span class="p">[</span>get_clocks sys_clk_100mhz<span class="p">]</span> -min 1.0 <span class="p">[</span>get_ports <span class="p">{</span>dac_data<span class="p">[</span>*<span class="p">]}]</span>

<span class="c"># False paths for reset signals (asynchronous resets don't need timing analysis). Also used if proper clock domain crossing circuits are being used - basically, avoids timing analysis.
</span>set_false_path -from <span class="p">[</span>get_ports sys_reset_n<span class="p">]</span>

<span class="c"># Multicycle paths (if there is logic that takes multiple clock cycles, e.g. pipelining or time-multiplexing expensive operations).
</span>set_multicycle_path -setup 2 -from <span class="p">[</span>get_pins slow_logic_reg*/C<span class="p">]</span> -to <span class="p">[</span>get_pins output_reg*/D<span class="p">]</span>
set_multicycle_path -hold 1 -from <span class="p">[</span>get_pins slow_logic_reg*/C<span class="p">]</span> -to <span class="p">[</span>get_pins output_reg*/D<span class="p">]</span>
</code></pre></div></div> <p>Above definitions will be required for Vivado to provide an accurate timing analysis.</p> <p>After implementation, review the following:</p> <ul> <li>Timing Summary: All timing constraints should be met. No negative slack. Failure to meet timing requires redesign, possibly adding latches between long datapaths (beyond the scope of this guide).</li> <li>Utilization Report: Aim for at most 80%, though many designers typically shoot for 60%-70%. You can run <code class="language-plaintext highlighter-rouge">report_qor_assessment</code> to get exact thresholds (defined by Xilinx/AMD) for utilization targets; staying below these values gives the best QoR (quality of results) and easier timing closure. As utilization approaches 100%, the place and route tools have to work much harder to meet timing; this can also result in longer build times.</li> <li>Power Consumption: Verify it is within board capabilities. The system that you are integrating into likely has additional restrictions for power utilization that will need to be followed.</li> </ul> <hr/> <h1 id="debugging-workflow-over-jtag">Debugging Workflow over JTAG</h1> <p>This instruction is intended for reference when revising an IP that exists (or will exist) within a larger block diagram. This allows for debugging using the XSCT console in Vitis as well as the Vivado ILA (Integrated Logic Analyzer). Overall goal is to verify functionality of an FPGA build by reading/manipulating registers and viewing the waveform outputs prior to adding the complexity of an OS (and related software code) to the design. This instruction assumes a Zynq processor or similar is on the chip, and there is/will be a software layer of some sort on that processor, which applies to most modern FPGAs. This instruction also assumes all of the simulation work has been completed, which is beyond the scope of this guide.</p> <hr/> <h2 id="ip-creation">IP Creation</h2> <p>In Vivado, design and simulate the RTL code for the IP. Once this is satisfactory, select <strong>Implementation &gt; Run Implementation</strong> from the Flow Navigator (left panel in Vivado GUI).</p> <p>Once implementation is complete, go to <strong>Tools &gt; Package New IP</strong>. Click through the prompts.</p> <ul> <li>Select <strong>Packaging Options &gt; Package your current project</strong> on the second page.</li> <li>On the third page, choose an IP location that your overall block diagram project can pull from (here, <code class="language-plaintext highlighter-rouge">src/ip/</code>), and create a sub-folder specifically for this IP, named appropriately. Select this folder as the location. <em>If rebuilding an existing IP, it is important to overwrite the same folder so that the block diagram automatically updates</em>.</li> <li>Once the Package IP tab comes up, take care of any issues. The <strong>Addressing and Memory</strong> tab will allow for registers to be mapped out (select the proper size for the register in question if needed, so it is not taking up too much address space). After this, the File Groups tab allows you to merge changes from the wizard. Finally, go to the <strong>Review and Package</strong> tab and select <strong>Re-package IP</strong>. The temporary IP project will close.</li> </ul> <hr/> <h2 id="block-diagram-implementation">Block Diagram Implementation</h2> <p>Please note that block diagrams can also be implemented within a top-level RTL. The constraints will need to be set up accordingly for whichever the top level design is.</p> <p>For first-time implementation only:</p> <ul> <li>Go to the <strong>IP Catalog</strong> in the Flow Navigator and right-click the background of the primary window. Select <strong>Add Repository</strong> and add the location of the custom IP to the project. Then select <strong>Open Block Design</strong> and add the IP to the block diagram. Connect as necessary in the <strong>Diagram</strong> tab, and ensure the addressing is set up properly for any registers in the <strong>Address Editor</strong> tab.</li> </ul> <p>Debug Core Setup:</p> <p><em>(Note: This section is written specifically for SoC processor integration.)</em></p> <ul> <li>Right click connections in the diagram that you would like debug probes to be added to, and select <strong>Debug</strong>. This can be done to entire AXI/AXIS connections, or simple I/Os.</li> <li>Add a Zynq processor IP block to the design. Double-click the Zynq processor block to configure, and navigate to the <strong>PS-PL Configuration</strong>. Within the <strong>PS-PL Cross Trigger</strong> interface, enable <strong>Input Cross Trigger &gt; Cross Trigger Input 0 &gt; CPU0 DBG REQ</strong> and <strong>Output Cross Trigger &gt; Cross Trigger Output 0 &gt; CPU0 DBG ACK</strong>.</li> <li>Select <strong>Run Connection Automation</strong> for the block diagram. It will connect the triggers to the Zynq processor, as well as the debug connections, to the ILA block that is automatically added. Double-click to configure the ILA block as needed. <em>This also needs to be done if any new debug cores are added</em>.</li> <li>Now go to <strong>Flow Navigator &gt; Open Synthesized Design</strong> and select <strong>Set Up Debug</strong>. Add the corresponding nets that were added previously. <em>*This also needs to be done if any new debug cores were added</em>.</li> <li>Finally, <strong>Generate Bitstream</strong>, and this should save the bitstream in the project’s <code class="language-plaintext highlighter-rouge">projectname.runs/impl_1/</code> folder.</li> </ul> <p>For subsequent implementations:</p> <ul> <li>Vivado will detect the IP was upgraded and will prompt you to go to <strong>Report IP Status</strong>, then <strong>Upgrade the IP</strong>. Edits to the block diagram and ILA nets may be needed for some changes (reference the above italics).</li> </ul> <hr/> <h2 id="connecting-to-hardware-and-programming">Connecting to Hardware and Programming</h2> <p>If the target is local, you can simply <strong>Connect to the Local Target</strong>. This applies only when the FPGA board is connected directly to the computer running Vivado via a JTAG cable (note: newer Xilinx development boards have built-in JTAG functionality and this cable is now simply a USB-type cable).</p> <p>Most likely, the target is connected to a remote server. For Xilinx Vitis/Vivado debug purposes, it will need to be connected via the JTAG cable or interface.</p> <p>Follow these instructions to connect to a remote hardware server:</p> <ul> <li>First, ssh into the server that the FPGA board is connected to via a USB-to-JTAG connection. Ensure the board’s switches are configured to allow for JTAG programming (which varies by board). So the connection stays awake even without user input, run the following command to ssh into the server (with appropriate username and IP):</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-X</span> <span class="nt">-o</span> <span class="nv">ServerAliveInterval</span><span class="o">=</span>30 username@SERVER.IP.GOES.HERE
</code></pre></div></div> <ul> <li>Next, enter the following command to enable the Vivado hardware server:</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hw_server
</code></pre></div></div> <ul> <li>Back within Vivado, type the following in the GUI’s XSCT console to connect to the remote hardware server (with the appropriate IP):</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>connect_hw_server <span class="nt">-url</span> SERVER.IP.GOES.HERE
</code></pre></div></div> <ul> <li>Once connected, you should be in the <strong>Hardware Manager</strong>. Open it if not, then right click the board and select <strong>Open Target</strong>. The board should be visible in <strong>Hardware</strong> under the connected IP value.</li> <li>Select <strong>Program Device</strong>, select the exported bitstream file from earlier, and the exported <code class="language-plaintext highlighter-rouge">.ltx</code> file from the debug build. It should be in the project folder under <code class="language-plaintext highlighter-rouge">projectname.runs/impl_1/</code> and was generated with the <strong>Set Up Debug</strong> wizard, during Implementation. Click <strong>Program</strong>.</li> <li>If all debug probes look good, go to <strong>File &gt; Export &gt; Export Hardware</strong>. Export a <strong>Fixed Platform</strong>, and <strong>Include Bitstream</strong>.</li> <li>Now, go to <strong>Tools &gt; Launch Vitis IDE</strong>. Leave Vivado and its Synthesis/Debug view open.</li> </ul> <p>Note: Failing to properly close hw_server on the remote machine can cause issues with trying to start a new instance of hw_server. If you get the error <code class="language-plaintext highlighter-rouge">Cannot create listening port: Socket bind error. Address already in use</code>, do the following to kill the process on the remote machine (assuming Linux), replacing 9085 here with the PID value returned with <code class="language-plaintext highlighter-rouge">pidof</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pidof hw_server
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">kill</span> <span class="nt">-9</span> 9085 <span class="c"># Replace 9085 with the correct PID value.</span>
</code></pre></div></div> <hr/> <h2 id="configuring-vitis-for-hardwareila-debug">Configuring Vitis for Hardware/ILA Debug</h2> <ul> <li>The cleanest way to do this is open a new workspace each time, since this avoids (and even fixes) numerous errors and snags that can occur. Typical method is to use the same folder but delete all contents prior to opening - you can also choose a new workspace each time if preferred. Once the workspace folder is selected, click <strong>Launch</strong>.</li> <li>Click <strong>File &gt; New &gt; New Application Project</strong>. Go to the <strong>Create a new platform from hardware (XSA)</strong> tab and load in the exported <code class="language-plaintext highlighter-rouge">.xsa</code> hardware file. Load the Hello World template and click <strong>Finish</strong>.</li> <li>In the bottom left <strong>Explorer</strong> panel, right click the <code class="language-plaintext highlighter-rouge">&lt;app_project_name&gt;_system</code> entry (should be second from the top) and click <strong>Build Project</strong>. This generates the necessary build files.</li> <li>Click the arrow next to the bug icon (“debug”) and select <strong>Debug Configurations</strong>. Double click <strong>Single Application Debug</strong> to create a custom debug application.</li> <li>In the <strong>Main</strong> tab, choose the appropriate connection. You may need to create a <strong>New</strong> one if the board is not local. The connection will save its variables even if you clear the workspace, but <em>this needs to be selected each time</em>.</li> <li>In the <strong>Target Setup</strong> tab, make sure all of the reset, program and run checkboxes are enabled (not Skip Revision Check) and then also <strong>Enable Cross Triggering</strong>. Modify this by clicking the <strong>…</strong> button.</li> <li>For <strong>Cross Trigger Breakpoints</strong>, create two. First, select <strong>CPU-0 &gt; CPU0 DBGACK</strong> in the left tab, and the entirety of <strong>FTM</strong> on the right.</li> <li>For the second breakpoint, select <strong>CPU-0 &gt; CPU0 debug request</strong> on the right, and the entirety of <strong>FTM</strong> on the left.</li> <li>Now click <strong>Debug</strong> to close the <strong>Debug Configurations</strong> window.</li> <li>At the arrow next to the bug icon again, click <strong>Debug As</strong> and select the configuration that was just generated. The FPGA should program, restart, and run.</li> </ul> <hr/> <h2 id="debugging-the-hardware-with-ila-and-register-readwrite">Debugging the Hardware with ILA and Register Read/Write</h2> <p>Back in Vivado, click <strong>Window &gt; Debug Probes</strong>. The play button can be used to set up triggers that will configure it to read the waveform when the conditions are met (typically, you can set a valid signal low-to-high (R) to trigger for AXI signals). The double arrow symbol can be used to immediately show the waveform at the current moment regardless of triggers.</p> <p>In Vitis, the memory window on the left side can be used to view memory addresses. Using the following commands allows you to read and write from these registers, which in turn will correspond with the registers in your design (which is currently running on the hardware itself) if the address is mapped properly.</p> <p>Example read command (can use -force flag if the register is protected):</p> <div class="language-tcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mrd 0x50000000
</code></pre></div></div> <p>Example write command (writes a hex value of 1 to addr 5000_0000):</p> <div class="language-tcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mwr 0x50000000 0x00000001
</code></pre></div></div> <p>While debugging with the ILA, pay attention to set up and hold time violations as well as clock domain crossing issues. Both are beyond the scope of this guide.</p> <hr/> <h2 id="debugging-the-software-with-vitis-and-the-integrated-logic-analyzer-ila">Debugging the Software with Vitis and the Integrated Logic Analyzer (ILA)</h2> <p>Create a <strong>New Application Project</strong> in Vitis as before, but set it as a blank C++ application and not Hello World. Add your software code in Vitis and build. The debugging tools are similar to the Eclipse IDE in that you can set breakpoints and view the machine code in Disassembly view.</p> <p>Assuming the hardware is connected via remote hw_server, you will need to open up a second terminal into that server. Set up minicom and connect to the board via UART (you will need a UART-to-USB connection in addition to the JTAG-to-USB connection) over 115200 baud, 8N1 and clear out modem settings A through H. This is typically <code class="language-plaintext highlighter-rouge">/dev/ttyUSB0</code> or some increment thereof. Any print outputs triggered by the C++ code in Vitis will be displayed over this terminal. Continue to modify code, rebuild the Application Project, and run as needed.</p> <hr/> <h2 id="code-examples">Code Examples</h2> <p>The following is listed here as a quick reference for creating register manipulation debug code.</p> <h3 id="axi-4-interface-protocol">AXI-4 Interface Protocol</h3> <p>For Xilinx devices, the AXI-4 interface is a common data transfer method between FPGA modules. The AXI-4 Lite protocol (most common) will typically load a correct series of values on the master side, and set <code class="language-plaintext highlighter-rouge">valid</code> to high when done. The slave side will set <code class="language-plaintext highlighter-rouge">ready</code> high, and this handshake will allow a single burst of (typically 32-bit) data to transfer. The full AXI functionality adds burst and data protection (prot) and is outside the scope of the below snippet, but below will work on both AXI-4 and AXI-4 Lite.</p> <p>For a system controlled by AXI interfaces, the FPGA will map these interfaces to registers. For example, 0x8000_0000 and 0x8001_0000 may be mapped as the base addresses to two AXI registers, and the registers can both be written to directly from software by using commands such as what is shown below. The system automatically handles all of the handshakes required, so the programmer only has to worry about mapping the register and then reading or writing to it.</p> <h4 id="without-an-os-vitis">Without an OS (Vitis)</h4> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">axi_write_phase</span><span class="p">(</span><span class="n">u32</span> <span class="n">writeValue</span><span class="p">){</span>
                <span class="n">Xil_Out32</span><span class="p">(</span><span class="n">AXI_SDR_ADDR</span><span class="p">,</span> <span class="n">writeValue</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <hr/> <h2 id="re-configuring-the-integrated-logic-analyzer">Re-Configuring the Integrated Logic Analyzer</h2> <p>When adding and removing debug cores, the design can end up getting buggy. Some notes on fixing:</p> <ul> <li>When adding nets, it is easy to mark them in netlist view (Right click &gt; <strong>Mark for Debug</strong>) and add them this way. When needing to debug a different set of nets, clear out the target constraints file of all synthesis-added parameters (these will be appended to the file and were for the debug cores). Then close the project and delete the <code class="language-plaintext highlighter-rouge">&lt;project_name&gt;.hw</code> and <code class="language-plaintext highlighter-rouge">&lt;project_name&gt;.runs</code> folders. This forces the project to do a clean synthesis, at which point you can add new debug cores. Then build as normal.</li> </ul> <hr/> <h1 id="debugging-workflow-on-os">Debugging Workflow on OS</h1> <p>Once the JTAG debug process has been proven functional, and the design is intended to run with an OS on the SoM, the next step is to build the software code on the OS and run it. To do this, the FPGA will need to be booted from an SD card containing both the firmware and the OS. Previously, it has been booting from JTAG.</p> <p>(Note: Creating and setting up a bootable OS is beyond the scope of this guide).</p> <p>Once the necessary files are loaded onto the SD card, ensure the boot select pins on the board are set to SD rather than JTAG, and power up the board. You can use minicom over USB to log in initially, and once SSH is set up, use SSH to enter the board and copy the necessary files over. QPSI-flash (Quad SPI) will need to be set up to allow persistence after boot. Software files should be built on the board itself, and then ran.</p> <p>Code for writing to registers on Linux is shown below for reference. From this point on, a designer will likely be manipulating registers from software and possibly setting up logging functionality to run on the processor.</p> <h4 id="with-an-os-running-cc-code-on-a-linux-os-on-the-zynq-processor">With an OS (Running C/C++ Code on a Linux OS on the Zynq Processor)</h4> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">axi_write_phase</span><span class="p">(</span><span class="kt">uint32_t</span> <span class="n">writeValue</span><span class="p">){</span>
                <span class="cm">/* Map the memory */</span>
                <span class="kt">int</span> <span class="n">fd</span> <span class="o">=</span> <span class="n">open</span><span class="p">(</span><span class="s">"/dev/mem"</span><span class="p">,</span> <span class="n">O_RDWR</span><span class="o">|</span><span class="n">O_SYNC</span><span class="p">);</span> <span class="c1">// Need to close this when finished to avoid memory issues.</span>
                <span class="cm">/* Map the SDR regs */</span>
                <span class="kt">void</span> <span class="o">*</span><span class="n">mm_sdr</span> <span class="o">=</span> <span class="n">mmap</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">4096</span><span class="p">,</span> <span class="n">PROT_READ</span> <span class="o">|</span> <span class="n">PROT_WRITE</span><span class="p">,</span> <span class="n">MAP_SHARED</span><span class="p">,</span> <span class="n">fd</span><span class="p">,</span> <span class="n">AXI_SDR_ADDR</span><span class="p">);</span>
                <span class="k">volatile</span> <span class="kt">int</span> <span class="o">*</span><span class="n">sdr_regs</span> <span class="o">=</span> <span class="p">(</span><span class="k">volatile</span> <span class="kt">int</span> <span class="o">*</span><span class="p">)</span><span class="n">mm_sdr</span><span class="p">;</span>
                <span class="o">*</span><span class="p">(</span><span class="n">sdr_regs</span><span class="o">+</span><span class="mi">0</span><span class="p">)</span> <span class="o">=</span> <span class="p">(</span><span class="n">writeValue</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>Note: For the above, you will eventually need something to close the file descriptor, such as:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">munmap</span><span class="p">(</span><span class="n">mm_sdr</span><span class="p">,</span> <span class="mi">4096</span><span class="p">);</span>
<span class="n">close</span><span class="p">(</span><span class="n">fd</span><span class="p">);</span>
</code></pre></div></div> <hr/> <h1 id="conclusion">Conclusion</h1> <p>That summarizes the general implementation and debug workflow from post-synthesis to on-hardware implementation. Please feel free to reach out if you find any mistakes or inaccuracies in this guide.</p> <hr/> <h1 id="acronyms">Acronyms</h1> <ul> <li>APU: Application Processing Unit</li> <li>RPU: Real-Time Processing Unit</li> <li>RTL: Register Transfer Level</li> <li>HLS: High Level Synthesis</li> <li>PLL: Phase-Locked Loop</li> <li>MMCM: Mixed-Mode Clock Manager</li> <li>RAM: Random Access Memory</li> <li>DMA: Direct Memory Access</li> <li>ADC: Analog to Digital Converter</li> <li>DAC: Digital to Analog Converter</li> <li>FPGA: Field Programmable Gate Array</li> <li>MPSoC: Multi Processor System on Chip</li> <li>DDR: Double Data Rate</li> <li>SoM: System On Module</li> <li>QoR: Quality of Results</li> </ul>]]></content><author><name>Joshua Rothe</name></author><category term="fpga"/><category term="fpga"/><category term="xilinx"/><category term="amd"/><category term="vivado"/><summary type="html"><![CDATA[Guide that walks through a build and debug workflow for Xilinx/AMD SoC FPGAs, utilizing the Zynq processor on the board.]]></summary></entry><entry><title type="html">Setting Up a WireGuard VPN Client on Linux</title><link href="https://portfolio.rothellc.com/blog/2025/setting-up-a-wireguard-vpn-client-on-linux/" rel="alternate" type="text/html" title="Setting Up a WireGuard VPN Client on Linux"/><published>2025-09-27T00:00:00+00:00</published><updated>2025-09-27T00:00:00+00:00</updated><id>https://portfolio.rothellc.com/blog/2025/setting-up-a-wireguard-vpn-client-on-linux</id><content type="html" xml:base="https://portfolio.rothellc.com/blog/2025/setting-up-a-wireguard-vpn-client-on-linux/"><![CDATA[<p><img src="../../../assets/img/diagrams/2025-09-27-setting-up-a-wireguard-vpn-client-on-linux/wireguard-vpn-client-diagram.svg" alt="WireGuard Network Diagram" class="img-fluid"/></p> <hr/> <p>Setting up a WireGuard VPN for privacy and security involves setting up both server and client side systems. This guide explains how to set up a client side Linux system - with or without <a href="https://pi-hole.net/">Pi-hole DNS filtering</a> on the home network - and then configure the system so that WireGuard settings will switch depending on if the client system is on the home network or not. This is necessary because the WireGuard client will break your network connection if you are on your home network, and there is no need to manually switch your VPN client on and off when automation exists.</p> <p>This guide assumes a <a href="https://www.wireguard.com/quickstart/">WireGuard VPN server</a> is set up, and port forwarding is configured on the home router (UDP, port 51820 should be forwarding to your WireGuard server). This guide is also written for Linux Mint - while this should also work for most Debian systems, you may need to modify some file paths depending on your distro.</p> <p>Note: This post was updated Oct 18 2025 to fix two critical issues with the original approach: the WireGuard client would break upon switching network types while the client was shut down, and it did not check if the WireGuard server itself was reachable when on an external network prior to enabling. This improved method fixes these issues and keeps the WireGuard client disabled until the following is verified:</p> <ol> <li>The client is not on the home network.</li> <li>The WireGuard server is reachable.</li> </ol> <p>The old method is kept in the Appendix for historical purposes, but is depreciated.</p> <hr/> <h1 id="background">Background</h1> <p>VPNs are a great tool for security and privacy, with key benefits being:</p> <p><strong>Privacy and Security:</strong></p> <ul> <li><strong>Location Privacy</strong>: All traffic appears to be from your home IP address. Your actual location is obscured to anyone tracking your browsing activity.</li> <li><strong>Data Protection</strong>: On public wireless networks, your VPN tunnel ensures packet sniffers and other malicious actors can’t see your activity.</li> <li><strong>Audited:</strong> WireGuard is open source and is <a href="https://courses.csail.mit.edu/6.857/2018/project/He-Xu-Xu-WireGuard.pdf">audited regularly</a> by both organizations and individuals. All of its code is viewable by any developer curious enough to know how it functions.</li> </ul> <p><strong>Functionality:</strong></p> <ul> <li><strong>Ad Blocking:</strong> If you run a <a href="https://pi-hole.net/">Pi-Hole</a> system at home for ad blocking, you can use that same tool while off your home network.</li> <li><strong>Home Network Access:</strong> This secure tunnel to your home network means you can access devices on your home network such as cameras, NAS systems, and IoT devices without needing to expose them to the internet.</li> <li><strong>Speed:</strong> <a href="https://www.wireguard.com/performance/">WireGuard is fast</a>, faster than most other comparable VPN services.</li> </ul> <p><strong>Practicality:</strong></p> <ul> <li><strong>Low Overhead:</strong> Works on virtually all modern hardware.</li> <li><strong>Cryptographically Secure:</strong> Uses proven, modern cryptographic primitives detailed on the <a href="https://www.wireguard.com/">WireGuard homepage</a>.</li> <li><strong>Free:</strong> Self explanatory. No risk of subscription fees in the future, either. Open source comes with benefits!</li> </ul> <p>If you do not have a WireGuard VPN server set up and find this interesting, I’ve worked through the <a href="https://www.wireguard.com/quickstart/">official WireGuard documentation</a> and found it more than sufficient. Make sure this is complete before setting up clients! Windows and MacOS have <a href="https://www.wireguard.com/install/">dedicated programs</a> for clients, but Linux is a bit more complicated; hence this guide.</p> <hr/> <h1 id="prerequisites">Prerequisites</h1> <ul> <li>WireGuard server, configured with port forwarding on your home router.</li> <li>Linux client with <code class="language-plaintext highlighter-rouge">sudo</code> access (tested here on Mint/Debian).</li> <li>Basic command line familiarity.</li> </ul> <hr/> <h1 id="client-setup">Client Setup</h1> <h2 id="ssh-key-generation">SSH Key Generation</h2> <p>This process should be familiar (for general server access, not WireGuard-specific handshakes), but direction is provided below just in case. On the client, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-C</span> <span class="s2">"your-email@example.com"</span>
</code></pre></div></div> <p>Hit enter twice - use default location and no passkey (unless you really want one).</p> <p>There are two ways to copy your key to the server, the <strong>Easy Way</strong> and the <strong>More Likely Way</strong>.</p> <hr/> <div class="row"> <div class="col-md-6"> <h3 id="the-easy-way">The Easy Way:</h3> <p>The easy way only works if your client can access the server. Since we are generating keys, you should probably have password authentication enabled for SSH. In case you forgot, you modify these lines in <code class="language-plaintext highlighter-rouge">/etc/ssh/sshd_config</code>:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PasswordAuthentication no
PubkeyAuthentication <span class="nb">yes
</span>PermitRootLogin no
</code></pre></div> </div> <p>Then on the client side you can simply run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-copy-id username@server-ip
</code></pre></div> </div> <p>This copies SSH key settings to the host, if security settings allow it.</p> </div> <div class="col-md-6"> <h3 id="the-more-likely-way">The More Likely Way:</h3> <p>Run this on the client:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> ~/.ssh/id_ed25519.pub
</code></pre></div> </div> <p>Get this clipboard item to the server however you like, probably using another system that does have SSH access, and append it to <code class="language-plaintext highlighter-rouge">~/.ssh/authorized_keys</code>. No need to disable the server’s security requirements this way.</p> </div> </div> <hr/> <h2 id="installation">Installation</h2> <p>Run on the client to install necessary dependencies:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install </span>wireguard resolvconf netcat-openbsd
</code></pre></div></div> <p>Once that is done, generate WireGuard keys (these are different than SSH keys - the former are needed to access the WireGuard server for configuration):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /etc/wireguard
<span class="nb">sudo umask </span>077
<span class="nb">sudo </span>wg genkey | <span class="nb">sudo tee </span>privatekey | <span class="nb">sudo </span>wg pubkey | <span class="nb">sudo tee </span>publickey
</code></pre></div></div> <p>Next, create the WireGuard config file on your client using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/wireguard/wg0.conf
</code></pre></div></div> <p>And insert the following text to create your client’s WireGuard network configuration file. This facilitates the key handshake your client system will do with the server:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Interface]
PrivateKey <span class="o">=</span> <span class="c"># Paste the contents of /etc/wireguard/privatekey here</span>
Address <span class="o">=</span> 10.0.0.2/24
DNS <span class="o">=</span> 1.1.1.1

<span class="o">[</span>Peer]
PublicKey <span class="o">=</span> <span class="c"># Your server's public key goes here.</span>
Endpoint <span class="o">=</span> your-server-ip:51820
AllowedIPs <span class="o">=</span> 0.0.0.0/0
PersistentKeepalive <span class="o">=</span> 25
</code></pre></div></div> <p>Note that you have three items to fill in above. You will need the PublicKey from the server, not from what you have here on the client. So hold off on that for now.</p> <p>The private key on the client can be acquired using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo cat</span> /etc/wireguard/privatekey
</code></pre></div></div> <p>The public key on the client (which will be needed on the server) can be acquired using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo cat</span> /etc/wireguard/publickey
</code></pre></div></div> <p>Now SSH into the WireGuard server, and edit the WireGuard config using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/wireguard/wg0.conf
</code></pre></div></div> <p>Append the following to the file:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Peer]
PublicKey <span class="o">=</span> <span class="c"># Paste your client's public key here.</span>
AllowedIPs <span class="o">=</span> 10.0.0.x/32
</code></pre></div></div> <p>For <code class="language-plaintext highlighter-rouge">AllowedIPs</code>, you will need to replace the <code class="language-plaintext highlighter-rouge">x</code>. For my setup, the server was .1, and I had two previous clients taking up .2 and .3, so I used .4. These are the IPs the WireGuard server uses to identify the peers on its network.</p> <p>Back on the client, edit the client’s WireGuard config using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/wireguard/wg0.conf
</code></pre></div></div> <p>Remember how we didn’t have the server’s public key last time? Fix that now:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Interface]
PrivateKey <span class="o">=</span> <span class="c"># Paste(d) the contents of /etc/wireguard/privatekey here</span>
Address <span class="o">=</span> 10.0.0.2/24
DNS <span class="o">=</span> 1.1.1.1

<span class="o">[</span>Peer]
PublicKey <span class="o">=</span> <span class="c"># Your server's public key goes here.</span>
Endpoint <span class="o">=</span> your-server-ip:51820
AllowedIPs <span class="o">=</span> 0.0.0.0/0
PersistentKeepalive <span class="o">=</span> 25
</code></pre></div></div> <p>Three items to check, above. Make sure the <code class="language-plaintext highlighter-rouge">Address</code> on your client side matches <code class="language-plaintext highlighter-rouge">AllowedIps</code> on the server side.</p> <hr/> <h1 id="automated-vpn-management">Automated VPN Management</h1> <p>Run this first to set the WireGuard client as disabled by default:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl disable wg-quick@wg0
<span class="nb">sudo </span>systemctl stop wg-quick@wg0
</code></pre></div></div> <p>Run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/NetworkManager/dispatcher.d/99-wireguard-auto
</code></pre></div></div> <p>Replace the script text with the following, adjusting the <code class="language-plaintext highlighter-rouge">HOME_*</code> values as needed:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="nv">INTERFACE</span><span class="o">=</span><span class="nv">$1</span>
<span class="nv">ACTION</span><span class="o">=</span><span class="nv">$2</span>

<span class="c"># Configuration. Change these as needed.</span>
<span class="nv">HOME_NETWORK_GATEWAY</span><span class="o">=</span><span class="s2">"192.168.1.1"</span> <span class="c"># Router's IP.</span>
<span class="nv">HOME_WIREGUARD_SERVER</span><span class="o">=</span><span class="s2">"192.168.1.114"</span> <span class="c"># WireGuard server IP.</span>
<span class="nv">WIREGUARD_PORT</span><span class="o">=</span><span class="s2">"51820"</span>
<span class="nv">HOME_EXTERNAL_IP</span><span class="o">=</span><span class="s2">"YOUR.EXTERNAL.IP"</span> <span class="c"># Your external home network IP.</span>
<span class="nv">LOG_FILE</span><span class="o">=</span><span class="s2">"/var/log/wireguard-auto.log"</span>

<span class="nv">PING_TIMEOUT</span><span class="o">=</span>5
<span class="nv">NC_TIMEOUT</span><span class="o">=</span>10

log_message<span class="o">()</span> <span class="o">{</span>
    <span class="nb">echo</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">: [</span><span class="nv">$INTERFACE</span><span class="s2">/</span><span class="nv">$ACTION</span><span class="s2">] </span><span class="nv">$1</span><span class="s2">"</span> | <span class="nb">tee</span> <span class="nt">-a</span> <span class="s2">"</span><span class="nv">$LOG_FILE</span><span class="s2">"</span>
<span class="o">}</span>

is_wireguard_server_reachable<span class="o">()</span> <span class="o">{</span>
    <span class="nb">local </span><span class="nv">server_ip</span><span class="o">=</span><span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">port</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
    
    <span class="k">if </span><span class="nb">command</span> <span class="nt">-v</span> nc <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
        if </span>nc <span class="nt">-u</span> <span class="nt">-z</span> <span class="nt">-w</span> 5 <span class="s2">"</span><span class="nv">$server_ip</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$port</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
            </span>log_message <span class="s2">"Server </span><span class="nv">$server_ip</span><span class="s2"> is reachable and responding"</span>
            <span class="k">return </span>0
        <span class="k">else
            </span>log_message <span class="s2">"WireGuard port </span><span class="nv">$port</span><span class="s2"> on </span><span class="nv">$server_ip</span><span class="s2"> is not responding"</span>
            <span class="k">return </span>1
        <span class="k">fi
    else</span>
        <span class="c"># Fallback if nc is not available.</span>
        log_message <span class="s2">"Error: nc not available"</span>
        <span class="k">return </span>1
    <span class="k">fi</span>
<span class="o">}</span>

is_home_network<span class="o">()</span> <span class="o">{</span>
    <span class="k">if </span>ping <span class="nt">-c</span> 3 <span class="nt">-W</span> <span class="s2">"</span><span class="nv">$PING_TIMEOUT</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$HOME_NETWORK_GATEWAY</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
        if </span>ping <span class="nt">-c</span> 3 <span class="nt">-W</span> <span class="s2">"</span><span class="nv">$PING_TIMEOUT</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$HOME_WIREGUARD_SERVER</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
            </span><span class="nv">local_ip</span><span class="o">=</span><span class="si">$(</span>ip route get 8.8.8.8 2&gt;/dev/null | <span class="nb">awk</span> <span class="s1">'{print $7; exit}'</span><span class="si">)</span>
            <span class="k">if</span> <span class="o">[[</span> <span class="s2">"</span><span class="nv">$local_ip</span><span class="s2">"</span> <span class="o">=</span>~ ^192<span class="se">\.</span>168<span class="se">\.</span>1<span class="se">\.</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then
                return </span>0
            <span class="k">fi
        fi
    fi
    return </span>1
<span class="o">}</span>

enable_wireguard<span class="o">()</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">!</span> systemctl is-active <span class="nt">--quiet</span> wg-quick@wg0<span class="p">;</span> <span class="k">then
        </span>log_message <span class="s2">"Enabling WireGuard"</span>
        systemctl <span class="nb">enable </span>wg-quick@wg0
        systemctl start wg-quick@wg0
    <span class="k">fi</span>
<span class="o">}</span>

disable_wireguard<span class="o">()</span> <span class="o">{</span>
    <span class="k">if </span>systemctl is-active <span class="nt">--quiet</span> wg-quick@wg0<span class="p">;</span> <span class="k">then
        </span>log_message <span class="s2">"Disabling WireGuard"</span>
        systemctl disable wg-quick@wg0
        systemctl stop wg-quick@wg0
    <span class="k">fi</span>
<span class="o">}</span>

<span class="c"># Only act on interface up events.</span>
<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span> <span class="o">!=</span> <span class="s2">"up"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">exit </span>0
<span class="k">fi

</span>log_message <span class="s2">"Network change detected on </span><span class="nv">$1</span><span class="s2">"</span>

<span class="nb">sleep </span>10

<span class="k">if </span>is_home_network<span class="p">;</span> <span class="k">then
    </span>log_message <span class="s2">"Home network confirmed - ensuring WireGuard is disabled"</span>
    disable_wireguard
<span class="k">else
    </span>log_message <span class="s2">"External network confirmed - testing VPN server"</span>
    <span class="k">if </span>is_wireguard_server_reachable <span class="s2">"</span><span class="nv">$HOME_EXTERNAL_IP</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$WIREGUARD_PORT</span><span class="s2">"</span><span class="p">;</span> <span class="k">then
        </span>log_message <span class="s2">"VPN server reachable - enabling WireGuard"</span>
        enable_wireguard
    <span class="k">else
        </span>log_message <span class="s2">"VPN server unreachable - WireGuard remains disabled"</span>
        disable_wireguard
    <span class="k">fi
fi</span>
</code></pre></div></div> <p>Next, run <code class="language-plaintext highlighter-rouge">sudo vim /etc/systemd/system/wireguard-boot-check.service</code> and add:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Unit]
<span class="nv">Description</span><span class="o">=</span>WireGuard Boot Config
<span class="nv">After</span><span class="o">=</span>network-online.target
<span class="nv">Wants</span><span class="o">=</span>network-online.target
<span class="nv">Before</span><span class="o">=</span>wg-quick@wg0.service

<span class="o">[</span>Service]
<span class="nv">Type</span><span class="o">=</span>oneshot
<span class="nv">ExecStart</span><span class="o">=</span>/usr/local/bin/wireguard-boot-check.sh
<span class="nv">RemainAfterExit</span><span class="o">=</span><span class="nb">yes
</span><span class="nv">TimeoutStartSec</span><span class="o">=</span>150

<span class="o">[</span>Install]
<span class="nv">WantedBy</span><span class="o">=</span>multi-user.target
</code></pre></div></div> <p>Then run <code class="language-plaintext highlighter-rouge">sudo vim /usr/local/bin/wireguard-boot-check.sh</code> and add the following adjusting variables as needed:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="c"># Configuration - adjust these to match your setup.</span>
<span class="nv">HOME_NETWORK_GATEWAY</span><span class="o">=</span><span class="s2">"192.168.1.1"</span>
<span class="nv">HOME_WIREGUARD_SERVER</span><span class="o">=</span><span class="s2">"192.168.1.114"</span>
<span class="nv">HOME_EXTERNAL_IP</span><span class="o">=</span><span class="s2">"YOUR.EXTERNAL.IP.HERE"</span>
<span class="nv">WIREGUARD_PORT</span><span class="o">=</span><span class="s2">"51820"</span>
<span class="nv">LOG_FILE</span><span class="o">=</span><span class="s2">"/var/log/wireguard-boot-check.log"</span>

<span class="c"># Timeout after 2 min.</span>
<span class="nv">NETWORK_WAIT_TIMEOUT</span><span class="o">=</span>120
<span class="nv">PING_TIMEOUT</span><span class="o">=</span>5
<span class="nv">NC_TIMEOUT</span><span class="o">=</span>10

log_message<span class="o">()</span> <span class="o">{</span>
    <span class="nb">echo</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">: </span><span class="nv">$1</span><span class="s2">"</span> | <span class="nb">tee</span> <span class="nt">-a</span> <span class="s2">"</span><span class="nv">$LOG_FILE</span><span class="s2">"</span>
<span class="o">}</span>

is_wireguard_server_reachable<span class="o">()</span> <span class="o">{</span>
    <span class="nb">local </span><span class="nv">server_ip</span><span class="o">=</span><span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">port</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>

    <span class="c"># Give network time to settle on boot.</span>
    <span class="nb">sleep </span>10
    check_timeout
    
    <span class="k">if </span><span class="nb">command</span> <span class="nt">-v</span> nc <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
        if </span>nc <span class="nt">-u</span> <span class="nt">-z</span> <span class="nt">-w</span> 5 <span class="s2">"</span><span class="nv">$server_ip</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$port</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
            </span>log_message <span class="s2">"Server </span><span class="nv">$server_ip</span><span class="s2"> is reachable and responding"</span>
            <span class="k">return </span>0
        <span class="k">else
            </span>log_message <span class="s2">"WireGuard port </span><span class="nv">$port</span><span class="s2"> on </span><span class="nv">$server_ip</span><span class="s2"> is not responding"</span>
            <span class="k">return </span>1
        <span class="k">fi
    else</span>
        <span class="c"># Fallback if nc is not available.</span>
        log_message <span class="s2">"Error: nc not available"</span>
        <span class="k">return </span>1
    <span class="k">fi</span>
<span class="o">}</span>

is_home_network<span class="o">()</span> <span class="o">{</span>
    <span class="c"># Check if home gateway is reachable.</span>
    <span class="k">if </span>ping <span class="nt">-c</span> 3 <span class="nt">-W</span> <span class="s2">"</span><span class="nv">$PING_TIMEOUT</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$HOME_NETWORK_GATEWAY</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then</span>
        <span class="c"># Second check by pinging WireGuard server directly.</span>
        <span class="k">if </span>ping <span class="nt">-c</span> 3 <span class="nt">-W</span> <span class="s2">"</span><span class="nv">$PING_TIMEOUT</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$HOME_WIREGUARD_SERVER</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then</span>
            <span class="c"># Third check by seeing if client IP is in the home range.</span>
            <span class="nv">local_ip</span><span class="o">=</span><span class="si">$(</span>ip route get 8.8.8.8 2&gt;/dev/null | <span class="nb">awk</span> <span class="s1">'{print $7; exit}'</span><span class="si">)</span>
            <span class="k">if</span> <span class="o">[[</span> <span class="s2">"</span><span class="nv">$local_ip</span><span class="s2">"</span> <span class="o">=</span>~ ^192<span class="se">\.</span>168<span class="se">\.</span>1<span class="se">\.</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then
                </span>log_message <span class="s2">"Confirmed home network (gateway: </span><span class="nv">$HOME_NETWORK_GATEWAY</span><span class="s2">, server: </span><span class="nv">$HOME_WIREGUARD_SERVER</span><span class="s2">, local IP: </span><span class="nv">$local_ip</span><span class="s2">)"</span>
                <span class="k">return </span>0
            <span class="k">fi
        fi
    fi
    </span>log_message <span class="s2">"Not on home network"</span>
    <span class="k">return </span>1
<span class="o">}</span>

enable_wireguard<span class="o">()</span> <span class="o">{</span>
    log_message <span class="s2">"Enabling WireGuard"</span>
    systemctl <span class="nb">enable </span>wg-quick@wg0
    systemctl start wg-quick@wg0
<span class="o">}</span>

log_message <span class="s2">"Boot check: Starting (WireGuard disabled by default)"</span>

<span class="c"># Wait for stable network connectivity.</span>
<span class="nv">elapsed</span><span class="o">=</span>0
<span class="k">while</span> <span class="o">[</span> <span class="nv">$elapsed</span> <span class="nt">-lt</span> <span class="nv">$NETWORK_WAIT_TIMEOUT</span> <span class="o">]</span><span class="p">;</span> <span class="k">do
    if </span>ping <span class="nt">-c</span> 2 <span class="nt">-W</span> 3 8.8.8.8 <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
        </span>log_message <span class="s2">"Network connectivity established after </span><span class="k">${</span><span class="nv">elapsed</span><span class="k">}</span><span class="s2">s"</span>
        <span class="nb">break
    </span><span class="k">fi
    </span>log_message <span class="s2">"Waiting for network connectivity... (</span><span class="k">${</span><span class="nv">elapsed</span><span class="k">}</span><span class="s2">s)"</span>
    <span class="nb">sleep </span>10
    <span class="nv">elapsed</span><span class="o">=</span><span class="k">$((</span>elapsed <span class="o">+</span> <span class="m">10</span><span class="k">))</span>
<span class="k">done</span>

<span class="c"># If still no connectivity, exit.</span>
<span class="k">if</span> <span class="o">!</span> ping <span class="nt">-c</span> 2 <span class="nt">-W</span> 3 8.8.8.8 <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then
    </span>log_message <span class="s2">"No network connectivity after </span><span class="k">${</span><span class="nv">elapsed</span><span class="k">}</span><span class="s2">s - WireGuard remains disabled"</span>
    <span class="nb">exit </span>0
<span class="k">fi

</span>log_message <span class="s2">"Allowing network to settle..."</span>
<span class="nb">sleep </span>15

<span class="c"># Network location detection.</span>
<span class="k">if </span>is_home_network<span class="p">;</span> <span class="k">then
    </span>log_message <span class="s2">"Home network confirmed - WireGuard remains disabled"</span>
<span class="k">else
    </span>log_message <span class="s2">"External network confirmed - testing VPN server accessibility"</span>
    <span class="k">if </span>is_wireguard_server_reachable <span class="s2">"</span><span class="nv">$HOME_EXTERNAL_IP</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$WIREGUARD_PORT</span><span class="s2">"</span><span class="p">;</span> <span class="k">then
        </span>log_message <span class="s2">"VPN server confirmed reachable - enabling WireGuard"</span>
        enable_wireguard
    <span class="k">else
        </span>log_message <span class="s2">"VPN server not accessible - WireGuard remains disabled"</span>
    <span class="k">fi
fi

</span>log_message <span class="s2">"Boot check: WireGuard configuration complete"</span>
</code></pre></div></div> <p>Make the script executable and enable it.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo chmod</span> +x /usr/local/bin/wireguard-boot-check.sh
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>wireguard-boot-check.service
</code></pre></div></div> <p>Lastly, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/wireguard/wg0.conf
</code></pre></div></div> <p>And add this to the file:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Interface]
PrivateKey <span class="o">=</span> <span class="c"># Your laptop's private key.</span>
Address <span class="o">=</span> 10.0.0.2/24
DNS <span class="o">=</span> 1.1.1.1 <span class="c"># Or 192.168.1.xxx for Pi-Hole.</span>

<span class="o">[</span>Peer]
PublicKey <span class="o">=</span> <span class="c"># Your server's public key.</span>
Endpoint <span class="o">=</span> your-server-external-ip:51820
AllowedIPs <span class="o">=</span> 0.0.0.0/0
PersistentKeepalive <span class="o">=</span> 25
</code></pre></div></div> <p>Note for Endpoint above, you need your server’s external IP. This will be your home router’s IP, port forwarded to your VPN server.</p> <hr/> <h2 id="verification">Verification</h2> <p>After setup is complete, verify your VPN is working correctly.</p> <p>On an ‘away’ network, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>wg show
<span class="nb">sudo </span>systemctl status wg-quick@wg0
ip addr show wg0
</code></pre></div></div> <p>You should see your WireGuard client configuration in the output.</p> <p>You should also see your home external IP displayed when you try to view your client’s public IP. Verify this using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> ifconfig.me
</code></pre></div></div> <p>Verify DNS resolution using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nslookup google.com
</code></pre></div></div> <p>The logs should also populate as your client switches networks or reboots. This can be viewed using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo tail</span> <span class="nt">-f</span> /var/log/wireguard-auto.log
<span class="nb">sudo tail</span> <span class="nt">-f</span> /var/log/wireguard-boot-check.log
</code></pre></div></div> <hr/> <h1 id="conclusion">Conclusion</h1> <p>That should get you a working VPN client! If you notice any issues with this guide, please feel free to reach out or leave a comment.</p> <hr/> <h1 id="appendix">Appendix</h1> <h2 id="dual-wireguard-configurations---old-method">Dual WireGuard Configurations - Old Method</h2> <p>The following method is depreciated and is kept for historical purposes only.</p> <p>On your client side, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo cp</span> /etc/wireguard/wg0.conf /etc/wireguard/wg0-away.conf
<span class="nb">sudo cp</span> /etc/wireguard/wg0.conf /etc/wireguard/wg0-home.conf
</code></pre></div></div> <p>Both of these files will become your home and away configurations, which will be copied into <code class="language-plaintext highlighter-rouge">wg0.conf</code> when your client detects a network change.</p> <p>Edit the home configuration using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/wireguard/wg0-home.conf
</code></pre></div></div> <p>The only change you need to make below is to AllowedIPs: <code class="language-plaintext highlighter-rouge">your-local-ip</code> (e.g. 192.168.1.0/24) is critical here, as you want the initial <code class="language-plaintext highlighter-rouge">192.168.1.*</code> to match the home network IPs your router issues. Also, don’t forget to adjust <code class="language-plaintext highlighter-rouge">Address</code> if needed.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Interface]
PrivateKey <span class="o">=</span> <span class="c"># Your laptop's private key.</span>
Address <span class="o">=</span> 10.0.0.2/24
DNS <span class="o">=</span> 1.1.1.1 <span class="c"># Or 192.168.1.xxx for Pi-Hole.</span>

<span class="o">[</span>Peer]
PublicKey <span class="o">=</span> <span class="c"># Your server's public key.</span>
Endpoint <span class="o">=</span> your-server-local-ip:51820
AllowedIPs <span class="o">=</span> your-local-ip/24
PersistentKeepalive <span class="o">=</span> 25
</code></pre></div></div> <p>Next, the away config should be edited using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/wireguard/wg0-away.conf
</code></pre></div></div> <p>And should contain:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Interface]
PrivateKey <span class="o">=</span> <span class="c"># Your laptop's private key.</span>
Address <span class="o">=</span> 10.0.0.2/24
DNS <span class="o">=</span> 1.1.1.1 <span class="c"># Or 192.168.1.xxx for Pi-Hole.</span>

<span class="o">[</span>Peer]
PublicKey <span class="o">=</span> <span class="c"># Your server's public key.</span>
Endpoint <span class="o">=</span> your-server-external-ip:51820
AllowedIPs <span class="o">=</span> 0.0.0.0/0
PersistentKeepalive <span class="o">=</span> 25
</code></pre></div></div> <p>Note for above, you need your server’s external IP. This will be your home router’s IP, port forwarded to your VPN server. This was likely set up when the WireGuard server was set up, and you only need a service like <a href="https://whatismyip.com">this</a> to check your external IP.</p> <p>As a reminder, these two <code class="language-plaintext highlighter-rouge">wg0-*</code> scripts will replace wg0.conf dynamically depending on what network the client is on. This will be triggered by the detection script, which we will now build.</p> <hr/> <h3 id="detection-script">Detection Script</h3> <p>Run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/NetworkManager/dispatcher.d/99-wireguard-auto
</code></pre></div></div> <p>Add the following, adjusting the <code class="language-plaintext highlighter-rouge">HOME_*</code> values as needed:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="nv">INTERFACE</span><span class="o">=</span><span class="nv">$1</span>
<span class="nv">ACTION</span><span class="o">=</span><span class="nv">$2</span>

<span class="c"># Filter out interfaces we don't care about (loopback, virtual bridges, docker).</span>
<span class="k">case</span> <span class="s2">"</span><span class="nv">$INTERFACE</span><span class="s2">"</span> <span class="k">in
    </span>lo|virbr<span class="k">*</span><span class="p">|</span>docker<span class="k">*</span><span class="p">|</span>br-<span class="k">*</span><span class="p">)</span>
        <span class="nb">exit </span>0
        <span class="p">;;</span>
<span class="k">esac</span>

<span class="nv">LOG_FILE</span><span class="o">=</span><span class="s2">"/var/log/wireguard-auto.log"</span>

log_message<span class="o">()</span> <span class="o">{</span>
    <span class="nb">echo</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">: [</span><span class="nv">$INTERFACE</span><span class="s2">/</span><span class="nv">$ACTION</span><span class="s2">] </span><span class="nv">$1</span><span class="s2">"</span> | <span class="nb">tee</span> <span class="nt">-a</span> <span class="s2">"</span><span class="nv">$LOG_FILE</span><span class="s2">"</span>
<span class="o">}</span>

<span class="c"># Only act on interface up/down events for primary connections.</span>
<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$ACTION</span><span class="s2">"</span> <span class="o">!=</span> <span class="s2">"up"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$ACTION</span><span class="s2">"</span> <span class="o">!=</span> <span class="s2">"down"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">exit </span>0
<span class="k">fi</span>

<span class="c"># Skip if this is the WireGuard interface itself.</span>
<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$INTERFACE</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"wg0"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">exit </span>0
<span class="k">fi

</span>log_message <span class="s2">"Network change detected on </span><span class="nv">$INTERFACE</span><span class="s2">"</span>

<span class="c"># Let network settle.</span>
<span class="nb">sleep </span>2

<span class="c"># Configuration. Change these as needed.</span>
<span class="nv">HOME_NETWORK_GATEWAY</span><span class="o">=</span><span class="s2">"192.168.1.1"</span> <span class="c"># Router's IP.</span>
<span class="nv">HOME_WIREGUARD_SERVER</span><span class="o">=</span><span class="s2">"192.168.1.114"</span> <span class="c"># WireGuard server IP.</span>
<span class="nv">WG_INTERFACE</span><span class="o">=</span><span class="s2">"wg0"</span>

is_home_network<span class="o">()</span> <span class="o">{</span>
    <span class="c"># Check if home gateway is reachable.</span>
    <span class="k">if </span>ping <span class="nt">-c</span> 1 <span class="nt">-W</span> 3 <span class="s2">"</span><span class="nv">$HOME_NETWORK_GATEWAY</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then</span>
        <span class="c"># Double-check by pinging WireGuard server directly.</span>
        <span class="k">if </span>ping <span class="nt">-c</span> 1 <span class="nt">-W</span> 3 <span class="s2">"</span><span class="nv">$HOME_WIREGUARD_SERVER</span><span class="s2">"</span> <span class="o">&gt;</span>/dev/null 2&gt;&amp;1<span class="p">;</span> <span class="k">then</span>
            <span class="c"># Triple-check by seeing if our IP is in the home range.</span>
            <span class="nv">local_ip</span><span class="o">=</span><span class="si">$(</span>ip route get 8.8.8.8 2&gt;/dev/null | <span class="nb">awk</span> <span class="s1">'{print $7; exit}'</span><span class="si">)</span>
            <span class="k">if</span> <span class="o">[[</span> <span class="s2">"</span><span class="nv">$local_ip</span><span class="s2">"</span> <span class="o">=</span>~ ^192<span class="se">\.</span>168<span class="se">\.</span>1<span class="se">\.</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then
                return </span>0 <span class="c"># Home.</span>
            <span class="k">fi
        fi
    fi
    return </span>1 <span class="c"># Away.</span>
<span class="o">}</span>

get_wg_status<span class="o">()</span> <span class="o">{</span>
    <span class="k">if </span>systemctl is-active <span class="nt">--quiet</span> wg-quick@wg0<span class="p">;</span> <span class="k">then
        </span><span class="nb">echo</span> <span class="s2">"active"</span>
    <span class="k">else
        </span><span class="nb">echo</span> <span class="s2">"inactive"</span>
    <span class="k">fi</span>
<span class="o">}</span>

copy_config<span class="o">()</span> <span class="o">{</span>
    <span class="nb">local source</span><span class="o">=</span><span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span>
    <span class="k">if</span> <span class="o">[</span> <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$source</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
        </span><span class="nb">cp</span> <span class="s2">"</span><span class="nv">$source</span><span class="s2">"</span> /etc/wireguard/wg0.conf
        log_message <span class="s2">"Copied </span><span class="nv">$source</span><span class="s2"> to wg0.conf"</span>
        <span class="k">return </span>0
    <span class="k">else
        </span>log_message <span class="s2">"ERROR: </span><span class="nv">$source</span><span class="s2"> not found!"</span>
        <span class="k">return </span>1
    <span class="k">fi</span>
<span class="o">}</span>

<span class="c"># Only proceed if network connection is present.</span>
<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$ACTION</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"up"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    if </span>is_home_network<span class="p">;</span> <span class="k">then
        </span>log_message <span class="s2">"Detected home network."</span>
        <span class="nv">current_status</span><span class="o">=</span><span class="si">$(</span>get_wg_status<span class="si">)</span>
        
        <span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$current_status</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"active"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then</span>
            <span class="c"># Check if using away config (away config has AllowedIPs = 0.0.0.0/0).</span>
            <span class="k">if </span><span class="nb">grep</span> <span class="nt">-q</span> <span class="s2">"AllowedIPs = 0.0.0.0/0"</span> /etc/wireguard/wg0.conf 2&gt;/dev/null<span class="p">;</span> <span class="k">then
                </span>log_message <span class="s2">"Switching to home VPN config."</span>
                systemctl stop wg-quick@wg0
                <span class="k">if </span>copy_config <span class="s2">"/etc/wireguard/wg0-home.conf"</span><span class="p">;</span> <span class="k">then
                    </span>systemctl start wg-quick@wg0
                <span class="k">fi
            else
                </span>log_message <span class="s2">"Already using home VPN config."</span>
            <span class="k">fi
        else
            </span>log_message <span class="s2">"Starting home VPN config."</span>
            <span class="k">if </span>copy_config <span class="s2">"/etc/wireguard/wg0-home.conf"</span><span class="p">;</span> <span class="k">then
                </span>systemctl start wg-quick@wg0
            <span class="k">fi
        fi
    else
        </span>log_message <span class="s2">"Detected external network."</span>
        <span class="nv">current_status</span><span class="o">=</span><span class="si">$(</span>get_wg_status<span class="si">)</span>
        
        <span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$current_status</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"active"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then</span>
            <span class="c"># Check if we're using the home config (home config has restricted AllowedIPs).</span>
            <span class="k">if </span><span class="nb">grep</span> <span class="nt">-q</span> <span class="s2">"AllowedIPs = 192.168.1.0/24"</span> /etc/wireguard/wg0.conf 2&gt;/dev/null<span class="p">;</span> <span class="k">then
                </span>log_message <span class="s2">"Switching to away VPN config."</span>
                systemctl stop wg-quick@wg0
                <span class="k">if </span>copy_config <span class="s2">"/etc/wireguard/wg0-away.conf"</span><span class="p">;</span> <span class="k">then
                    </span>systemctl start wg-quick@wg0
                <span class="k">fi
            else
                </span>log_message <span class="s2">"Already using away VPN config."</span>
            <span class="k">fi
        else
            </span>log_message <span class="s2">"Starting away VPN config."</span>
            <span class="k">if </span>copy_config <span class="s2">"/etc/wireguard/wg0-away.conf"</span><span class="p">;</span> <span class="k">then
                </span>systemctl start wg-quick@wg0
            <span class="k">fi
        fi
    fi
elif</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$ACTION</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"down"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then</span>
    <span class="c"># Uncomment the next line if wireguard should stop when network goes down.</span>
    <span class="c"># systemctl stop wg-quick@wg0</span>
    log_message <span class="s2">"Network disconnected."</span>
<span class="k">fi

</span><span class="nb">exit </span>0
</code></pre></div></div> <p>Once complete, make it executable:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo chmod</span> +x /etc/NetworkManager/dispatcher.d/99-wireguard-auto
</code></pre></div></div> <p>Then, add the following (adjust DNS IP as needed) using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>vim /etc/NetworkManager/dispatcher.d/99-wireguard-dns.sh
</code></pre></div></div> <p>Add:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="nv">INTERFACE</span><span class="o">=</span><span class="nv">$1</span>
<span class="nv">ACTION</span><span class="o">=</span><span class="nv">$2</span>

<span class="k">if</span> <span class="o">[[</span> <span class="s2">"</span><span class="nv">$INTERFACE</span><span class="s2">"</span> <span class="o">==</span> <span class="s2">"wg0"</span> <span class="o">&amp;&amp;</span> <span class="s2">"</span><span class="nv">$ACTION</span><span class="s2">"</span> <span class="o">==</span> <span class="s2">"up"</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then</span>
    <span class="c"># Set Pi-hole as DNS server for WireGuard interface.</span>
    resolvectl dns wg0 192.168.1.223 <span class="c"># Change! Pi-Hole DNS address or 1.1.1.1 1.0.0.1 (Cloudflare) or 9.9.9.9 149.112.112.112 (Quad9)</span>
    resolvectl domain wg0 <span class="s2">"~."</span>
    
    <span class="c"># Remove default route from WiFi interface to prioritize VPN DNS.</span>
    resolvectl default-route &lt;replace-me&gt; <span class="nb">false</span>
    
    <span class="c"># Ensure only VPN has default route for DNS.</span>
    resolvectl default-route wg0 <span class="nb">true</span>
    
    <span class="c"># Flush DNS cache to apply changes.</span>
    resolvectl flush-caches
    
    logger <span class="s2">"WireGuard DNS configured: Pi-hole via VPN with WiFi DNS disabled."</span>
<span class="k">fi</span>
</code></pre></div></div> <p>To know what to enter in <code class="language-plaintext highlighter-rouge">&lt;replace-me&gt;</code>, run <code class="language-plaintext highlighter-rouge">ip link show</code>. You need your actual WiFI interface name here.</p> <p>Make it executable:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo chmod</span> +x /etc/NetworkManager/dispatcher.d/99-wireguard-dns.sh
</code></pre></div></div> <p>To finalize, restart WireGuard on both machines. For the server first, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart wg-quick@wg0
</code></pre></div></div> <p>For the client, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>resolvectl default-route wlp4s0 <span class="nb">false
sudo </span>systemctl restart wg-quick@wg0
</code></pre></div></div> <p>This restarts WireGuard and fixes the initial VPN prioritization issue. Some Linux queries might go through your ISP’s DNS instead of the proper tunnel due to how systemd-resolve handles queries.</p>]]></content><author><name>Joshua Rothe</name></author><category term="linux"/><category term="devops"/><category term="linux"/><category term="vpn"/><category term="wireguard"/><category term="pihole"/><category term="devops"/><summary type="html"><![CDATA[Guide for configuring a client-side Linux (Debian) system for WireGuard VPN, automating network settings on both home and away networks.]]></summary></entry></feed>